# Unit testing stuffs for head.

project_test_files = [
    'whisker.cpp'
]

test('whisker_tests',
  executable(
    'run_whisker_tests',
    files(project_test_files),
    dependencies : [gtest_dep],
    install : false,
  )
)
