# Unit testing stuffs for head.

project_test_files = [
    'head.cpp'
]

test('head_tests',
  executable(
    'run_head_tests',
    files(project_test_files),
    dependencies : [gtest_dep],
    install : false,
  )
)
